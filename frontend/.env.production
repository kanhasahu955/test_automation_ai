# =============================================================================
# Production build (vite build) — used by the Docker image served via Nginx.
# Same-origin relative path so all /api/v1/* requests are proxied through
# the gateway Nginx (compose service `nginx` on :8080).
# =============================================================================
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY=
