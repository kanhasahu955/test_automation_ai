# Production build defaults. Vite picks this up automatically when running
# `npm run build` (e.g. inside the frontend Docker image).
# Use a same-origin relative path so requests flow through the gateway Nginx.
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY=
